# Measurement-dependency constraints for the Phase 3 regression policy.
#
# These versions match the committed calibration policy
# (benchmarks/regression/phase-3-regression-policy-v1.json:
#  environment.packages, EXCLUDING the package under test dbfbridge —
#  its version is provenance and must never gate comparability).
# The performance-regression workflow installs with:
#   python -m pip install -c benchmarks/regression/constraints-phase3-v1.txt -e ".[dev]"
#
# A candidate run with a different external measurement-dependency version is
# classified NOT_COMPARABLE (no performance claim may be made at all).
# This file reproduces the CALIBRATION recipe — it is not a
# latest-dependency requirements list.
dbf==0.99.11
dbfread==2.0.7
openpyxl==3.1.5
orjson==3.12.0
polars==1.44.1
psutil==7.2.2
xlsxwriter==3.2.9