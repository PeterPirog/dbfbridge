# Measurement-dependency constraints for the Phase 3 regression policy.
#
# These versions match the committed calibration dataset
# (benchmarks/regression/phase-3-regression-calibration-inputs.json).
# The performance-regression workflow installs with:
#   python -m pip install -c benchmarks/regression/constraints-phase3-v1.txt -e ".[dev]"
#
# A candidate run with a different measurement-dependency version is
# classified NOT_COMPARABLE (no performance claim may be made at all).
dbf==0.99.11
dbfread==2.0.7
openpyxl==3.1.5
orjson==3.12.0
polars==1.44.1
psutil==7.2.2
xlsxwriter==3.2.3